Installation
============

Library can be used via Poetry. Clone the repository and navigate to the root folder:

.. code-block:: console

    git clone https://github.com/olyatech/Interpolation.git
    cd Interpolation

Then install the package:

.. code-block:: console

    poetry install


